//1. Only one class (generate objects)
        //1. Yatch
        //2. StudentScore (pointer)
        //3. Bookshop (pointer)

//2. Parent & derived classes  (generate objects)
        //1. Animals(Dog, Cat, etc...)
        //2. Players(Warrior,Dwarf,etc...)
        //3. Shapes(Square, Rectangle, etc...)

//3. Parent & derived classes  (generate objects & objects + objects)
        //1. Aliens (offsprings)
        //2. 
